apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-resource
  annotations:
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  rules:
  - host: "34.135.240.58.nip.io"
    http:
      paths:
          - path: /?(.*)
            backend:
              serviceName: app-demo-ui-service
              servicePort: 3000
          - path: /api/?(.*)
            backend:
              serviceName: app-demo-ms-service
              servicePort: 8081
